<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ccr.mapper.AdminUserInfoMapper">

    <resultMap id="BaseResultMap" type="com.ccr.vo.AdminUserInfoVO">
        <id column="id" property="id"/>
        <result column="real_name" property="realName"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="email" property="email"/>
        <result column="avatar" property="avatar"/>
        <result column="gender" property="gender"/>
        <result column="status" property="status"/>
        <result column="user_type" property="userType"/>
        <association property="admin" javaType="com.ccr.entity.Admin" column="id" select="getAdminInfoByUserId"/>
    </resultMap>

    <update id="updateUserInfo">
        update user
        <set>
            <if test="password != null and password != ''"> password = #{password}, </if>
            <if test="realName != null and realName != ''"> real_name = #{realName}, </if>
            <if test="phone != null and phone != ''"> phone = #{phone}, </if>
            <if test="email != null and email != ''"> email = #{email}, </if>
            <if test="avatar != null and avatar != ''"> avatar = #{avatar}, </if>
            <if test="gender != null"> gender = #{gender}, </if>
        </set>
        where
            id = #{id} and is_deleted = 0
    </update>

    <select id="getAdminInfoByUserId" resultType="admin">
        select admin_no,department,position,hire_date,status from admin where user_id = #{id} and is_deleted = 0
    </select>

    <select id="getUserInfoById" resultMap="BaseResultMap">
        select id,real_name,phone,email,avatar,gender,status,user_type from user where id = #{currentId} and is_deleted = 0
    </select>

    <select id="selectById" resultType="com.ccr.entity.User">
        select id, username, password, real_name, phone, email, avatar, gender, status, user_type
        from user where id = #{currentId} and is_deleted = 0
    </select>

</mapper>